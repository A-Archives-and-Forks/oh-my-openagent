# Live capture, run 1 (isolated opencode server, real HTTP session creation)
#
# SCOPE OF THIS ARTIFACT - read before citing it:
#   SUPPORTS: "a real opencode process emits session.created when a session is
#             created over the HTTP API". That line came from an UNANCHORED
#             grep -q '"type":"session.created"' and is genuine.
#   DOES NOT SUPPORT: the top-level/parentID line below it. That check consumed
#             an ANCHORED grep ('{"type":"session.created"') which assumes
#             "type" is the first JSON key. The frame puts "type" last, so the
#             anchored grep matched nothing and the parentID branch could not
#             fail. It is a vacuous pass and is NOT evidence.
#
# The anchored grep is fixed in live-session-created.sh, and
# GREEN-assertions-can-fail.txt proves all three branches of the corrected
# assertion are reachable using a type-last frame.

sandbox: <tmp>/oqa-xdg.XXXXXX.JJnGbpkVUn  (the real opencode DB is untouched)
server: 127.0.0.1:64588
--- creating a real session over the HTTP API ---
POST /session -> {"id":"ses_f380eba8fffe2zA2V4ptZ33hKD","slug":"proud-cabin","projectID":"cc6c088c28ecc03bed8b0121d9b40f9c8b9c0678","dire

--- session.created frames observed on the wire ---

PASS: a real opencode process emitted session.created
PASS: the frame is top-level (no parentID), which is what the hook requires to fire
